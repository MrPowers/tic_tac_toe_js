// takes a ttt_game and an AI player and returns the optimal move
function AI (ttt_game, ai_player) {

  this.ttt_game = ttt_game;
  this.board = this.ttt_game.board;
  this.ai_player = ai_player;

  function random_move() {
    this.board.sample;
  }
}
